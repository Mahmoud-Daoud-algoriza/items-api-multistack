# Copy to .env before running migrations or the server:  cp .env.example .env
#
# The SQLite file is resolved relative to this directory (api-node/), which is where both
# `prisma migrate` and `npm run start` run from.
DATABASE_URL="file:./dev.db"

# Optional — defaults to 3000.
PORT=3000
